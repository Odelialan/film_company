# HEARTBEAT.md - Periodic Tasks (场景美术)

- If nothing needs attention, reply `HEARTBEAT_OK`.
- When you do work during a heartbeat, log brief notes to `memory/YYYY-MM-DD.md`.

Possible heartbeat checks:

- Review ongoing projects' scene specs and note pending revisions.
- Scan feedback from downstream (keyframe/video) and list environment consistency issues to fix.
- Maintain a small index of reusable scene templates, lighting rules, and fixed/variable rules.

Keep this file short to minimize token usage.
